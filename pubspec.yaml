name: local_config_storage
description: "Synchronously save and retrieve configurations in local persistent cache"
publish_to: 'none'
version: 1.0.0
environment:
  sdk: '>=3.3.1 <4.0.0'
dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0
flutter:
  uses-material-design: true